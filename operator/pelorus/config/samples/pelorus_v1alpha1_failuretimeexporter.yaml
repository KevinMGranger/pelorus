apiVersion: pelorus.konveyor.io/v1alpha1
kind: FailureTimeExporter
metadata:
  name: failuretimeexporter-sample
spec:
  # Default values copied from <project_dir>/helm-charts/exporters/values.yaml
  null
  
  
